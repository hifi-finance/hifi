import { Wallet } from "@ethersproject/wallet";

export default function shouldBehaveLikeMint(_admin: Wallet, _bob: Wallet, _eve: Wallet): void {
  describe("when the erc20 contract is compliant", async function () {
    /* TODO */
  });

  describe("when the erc20 contract is non-compliant", async function () {
    /* TODO */
  });
}
