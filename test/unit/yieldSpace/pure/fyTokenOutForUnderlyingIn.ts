import { expect } from "chai";

export default function shouldBehaveLikeFyTokenOutForUnderlyingIn(): void {
  context("when blah blah", function () {
    it("should work", function () {
      expect(2 + 2).to.equal(4);
    });
  });
}
